# Database connection (PostgreSQL URL or leave empty for SQLite fallback)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/traffic_db

# Redis connection (Redis URL or leave empty for in-memory fallback)
REDIS_URL=redis://localhost:6379/0

# Optional server configuration
PORT=8000
HOST=0.0.0.0
